import Link from 'next/link'; import { signup } from './actions'
export default async function Cadastro({searchParams}:{searchParams:Promise<Record<string,string|undefined>>}){const p=await searchParams;return <div className="auth-card"><div className="auth-logo">MTK <b>DIGITAL AI</b></div><h1>Crie sua conta</h1><p className="muted">Comece com 7 dias de trial e 100 créditos.</p>{p.erro&&<div className="alert error">{p.erro}</div>}<form className="form" action={signup}><label>Seu nome</label><input name="full_name" required/><label>E-mail</label><input name="email" type="email" required/><label>Senha</label><input name="password" type="password" minLength={6} required/><button className="btn" type="submit">CRIAR CONTA</button></form><p>Já tem conta? <Link href="/login">Entrar</Link></p></div>}
