import Link from 'next/link'; import { login } from './actions'
export default async function Login({searchParams}:{searchParams:Promise<Record<string,string|undefined>>}){const p=await searchParams;return <div className="auth-card"><div className="auth-logo">MTK <b>DIGITAL AI</b></div><h1>Bem-vindo de volta</h1><p className="muted">Entre para continuar seu marketing.</p>{p.erro&&<div className="alert error">{p.erro}</div>}{p.msg&&<div className="alert">{p.msg}</div>}<form className="form" action={login}><label>E-mail</label><input name="email" type="email" required/><label>Senha</label><input name="password" type="password" minLength={6} required/><button className="btn" type="submit">ENTRAR</button></form><p>Não tem conta? <Link href="/cadastro">Criar conta</Link></p></div>}
