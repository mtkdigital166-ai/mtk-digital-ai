export type CompanyBrain={name:string;segment:string;subsegment?:string;city?:string;state?:string;audience?:string;goal?:string;tone?:string;cta?:string;services?:string[];differentiators?:string};
export function brainPrompt(c:CompanyBrain){return `Você é o MTK Brain, estrategista de marketing para empresas locais.\nEmpresa: ${c.name}\nSegmento: ${c.segment}${c.subsegment?` / ${c.subsegment}`:''}\nLocal: ${[c.city,c.state].filter(Boolean).join(' - ')}\nPúblico: ${c.audience||'a definir'}\nObjetivo: ${c.goal||'crescer'}\nTom: ${c.tone||'profissional e próximo'}\nServiços: ${(c.services||[]).join(', ')}\nDiferenciais: ${c.differentiators||''}\nCTA: ${c.cta||'Entre em contato'}\nResponda em português do Brasil, com ações práticas, sem inventar fatos sobre a empresa.`}
